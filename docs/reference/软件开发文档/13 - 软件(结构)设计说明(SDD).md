---

标准: GB/T 8567-2006
版本: 1.0.0
生成时间: 2026-05-15
文档名称: 13 - 软件(结构)设计说明 (SDD)
项目名称: Obsidian 任务管理插件
'@ai-rewrite': preserve
'@ai-merge': append
'@ai-human-sections':
  - "## 全局数据结构说明"
  - "## 接口设计"
  - "## 任务整理处"
  - "## 所有任务日历图"
  - "## 所有任务甘特图"

---



说明：

1. 《软件(结构)设计说明》(SDD)描述了计算机软件配置项(CSCI)的设计。它描述了CSCI级设计决策、CSCI体系结构设计(概要设计)和实现该软件所需的详细设计。SDD可用接口设计说明IDD和数据库(顶层)设计说明DBDD加以补充。
2. SDD连同相关的IDD和DBDD是实现该软件的基础。向需方提供了设计的可视性，为软件支持提供了所需要的信息。
3. IDD和DBDD是否单独成册抑或与SDD合为一份资料视情况繁简而定。

# 引言

## 标识

## 系统概述

这是一个功能丰富的 Obsidian 任务管理插件，使用 Dataview 和 Tasks 插件 API 作为数据源，采用 ItemView 架构实现多个视图（矩阵、看板、日历、甘特图、时间线、任务表、整理箱等），并统一复用全局筛选条件、排序、左侧树导航。

插件的核心价值在于将 Obsidian 中的任务标记从纯文本转化为结构化数据，并通过多种可视化视图帮助用户高效管理任务。系统遵循“数据源分层、视图基类统一、全局状态共享”的架构原则，确保各视图之间筛选条件一致、交互体验统一。



## 文档概述

## 基线

# 引用文件

#  CSCI级设计决策

## 功能描述

本系统的核心功能包括：任务数据读取与解析、多视图展示（列表、看板、矩阵、日历、甘特图、时间线、整理箱等）、全局筛选与排序、任务编辑与批量修改、状态持久化。

系统按功能分为五个核心模块：

- **收集**：收集任务
- **整理**：整理任务
- **组织**：组织任务
- **回顾**：回顾任务
- **执行**：执行任务
- **统计**：统计任务

## 命名规范

项目采用统一的命名约定，确保代码风格一致。

| 元素         | 规则                       | 示例                        |
| ------------ | -------------------------- | --------------------------- |
| 文件名       | kebab-case                 | `base-task-view.js`         |
| 视图类型常量 | `VIEW_TYPE_` + UPPER_SNAKE | `VIEW_TYPE_CALENDAR`        |
| 类名         | PascalCase                 | `BaseTaskView`              |
| 函数/变量    | camelCase                  | `getAllTasks`               |
| CSS 类名     | kebab-case，视图前缀       | `.task-list`, `.cal-header` |

```yaml
命名规范:
  文件名: kebab-case
  视图类型常量: VIEW_TYPE_ + UPPER_SNAKE
  类名: PascalCase
  函数名: camelCase
  CSS类名: kebab-case，视图前缀

代码风格:
  缩进: 2空格
  引号: 单引号
  分号: 必须
  尾逗号: 始终
```



## 设计原则

本软件遵循以下设计原则，确保各模块之间职责清晰、协作高效：

1. **数据源分层**：优先使用 Tasks 插件 API（如 `fetchTasks`）获取任务列表，保证状态自动更新。对于 Tasks API 无法提供的自定义标记（如 `🆔`、`⛔`、`🏁` 等），通过 Dataview 的 `dv.pages` 和文件内容解析获取。

2. **视图基类与统一接口**：所有视图必须继承 `BaseTaskView`，实现 `_startCore(dv, app, storageAdapter)` 方法，并返回 `{ cleanup, updateSort }` 对象。基类负责创建 Dataview 代理、监听 `onOpen` / `onClose` 等生命周期。

3. **统一任务卡片组件**：`createTaskCard` 生成标准化 DOM：`<li class="task-item">` 包含任务描述和元数据行（状态图标、优先级、循环、日期、标签、ID、文件来源）。通过 `normalizeTaskCardData` 将 Tasks API 或 Dataview 结果转为统一格式，确保所有视图共享该卡片。

4. **全局筛选状态同步**：`panel.js` 维护全局 `state` 对象，包含日期范围、标记包含/排除、隐藏循环/已完成/取消等筛选条件。所有视图通过 `globalState` 获取当前筛选条件，筛选变更时统一调用 `refreshCurrentView` 刷新当前视图。

5. **样式复用约定**：全局 CSS 中预定义 `.view-grid`（两列/三列）、`.view-col`、`.task-list`、`.task-item`、`.col-header` 等类，避免重复定义。按钮样式统一使用 `quick-btn`、`sort-btn`、`cascade-btn` 等类名，激活状态加 `-active` 后缀。

6. **状态持久化**：全局筛选状态使用 Obsidian 的 `Plugin.loadData` / `saveData` 存储，确保重启后恢复。整理箱视图的编辑预览、历史快照等复杂状态采用 `localStorage` 或独立文件存储，防止视图刷新丢失。

7. **代码质量保障**：强制使用 ESLint + `eslint-plugin-obsidianmd` 进行检查。GitHub Action 自动运行测试和 lint，保证代码风格一致。

8. **性能与体验**：任务列表超过 100 条时分页（`PAGE_SIZE=50`）；甘特图使用 Canvas 虚拟滚动。复杂视图在数据处理阶段预计算时间戳和排序索引，避免渲染时重复计算。DOM 增量更新：骨架构建一次，后续仅替换内容部分。

9. **编辑与预览逻辑（整理箱专用）**：多级筛选按钮联动：一级确定后动态显示二级/三级选项。编辑按钮分组，支持多选并行修改，预览内容累积（下一个编辑操作基于上一个预览结果，而非原始文本）。单行“确定”仅切换界面状态（显示“已修改”），批量“保存所有修改”才写入文件；提供历史快照和撤回功能。

10. **任务标记规范**：任务格式严格遵循：`- [状态] 描述 🔼 🔁 every week ➕ 日期 ⏳ 日期 🛫 日期 📅 日期 ✅ 日期 ❌ 日期 🆔 id ⛔ id1,id2 🏁 flag`。自定义标记在 `read-tasks.js` 中通过正则解析，确保与 Tasks 插件兼容。

# CSCI体系结构设计

## 体系结构

## 4.程序(模块)划分

{在每个模块的自然语言描述末尾嵌入组件 YAML 块}

```yaml
组件:
  名称: {模块名称}
  描述: {模块功能描述}
  接口:
    - {提供的接口}
  依赖:
    - {依赖的其他模块}
```

## 程序(模块)层次结构关系

```
root/
├── main.js                         # 构建产物
├── manifest.json                   # 插件清单
├── styles.css                      # 全局样式
├── esbuild.config.mjs              # esbuild 打包配置
├── jest.config.cjs                 # Jest 配置
├── babel.config.js                 # Babel 配置
├── src/
│   ├── main.js                     # 插件入口：注册所有视图、命令，启动数据加载
│   ├── configs/
│   │   └── plugin-configs.js       # 全局常量：文件夹路径、状态图标映射、优先级映射、颜色、分页大小等
│   ├── panel/
│   │   ├── panel.js                # 导航中心 ItemView：头部固定栏、左侧任务树、全局 state 维护、视图切换逻辑
│   │   ├── bars/                   # UI 按钮栏组件
│   │   │   ├── control-botton-bar.js  # 控制栏（刷新、间隔模式、循环/完成/取消任务显隐开关）
│   │   │   ├── date-botton-bar.js     # 日期级联选择器（年/季/月/周/日）
│   │   │   ├── hide-botton-bar.js     # 筛选面板与任务树的显示/隐藏
│   │   │   ├── mark-botton-bar.js     # 执行状态筛选、标记包含/排除面板
│   │   │   ├── quick-botton-bar.js    # 快捷日期范围按钮 + 执行查询按钮
│   │   │   ├── side-botton-bar.js     # 左侧视图切换图标按钮（分组显示）
│   │   │   └── sort-botton-bar.js     # 排序栏（全局排序键 ALL_SORT_KEYS）
│   │   ├── components/
│   │   │   └── tree-view-components.js # 可复用的任务树组件（buildTree、flat、sort）
│   │   ├── interacts/
│   │   │   ├── chart-interact.js      # 图表缩放/拖拽交互
│   │   │   └── tooltip-interact.js    # 通用 Tooltip 管理器
│   │   └── views/                     # 所有子视图（均继承 BaseTaskView）
│   │       ├── base-task-view.js      # 视图基类 + createTaskCard + normalizeTaskCardData
│   │       ├── base-list-view.js      # 通用列表工厂函数
│   │       ├── inbox-task-view.js     # 收集箱
│   │       ├── kanban-task-view.js    # 看板
│   │       ├── matrix-task-view.js    # 四象限矩阵
│   │       ├── calendar-task-view.js  # 日历
│   │       ├── gantt-task-view.js     # 甘特图
│   │       ├── data-tasks-view.js     # 统计分析
│   │       ├── organize-task-view.js  # 整理箱（复杂编辑视图）
│   │       ├── tree-task-view.js      # 任务树
│   │       ├── important-task-view.js # 重要任务
│   │       ├── recurring-task-view.js # 循环任务
│   │       ├── today-task-view.js     # 今天任务
│   │       ├── overdue-task-view.js   # 逾期任务
│   │       ├── future-task-n-view.js  # 未来 15 天任务
│   │       ├── future-task-all-view.js# 全部未来任务
│   │       ├── timeline-task-view.js  # 时间线
│   │       ├── table-task-view.js     # 所有任务表
│   │       ├── tag-task-view.js       # 标签聚合
│   │       ├── depends-task-view.js   # 依赖任务
│   │       ├── pomodoro-task-view.js  # 番茄钟
│   │       └── ...                    # 其他辅助视图
│   ├── tasks/
│   │   ├── read/
│   │   │   └── read-tasks.js          # 统一任务读取：调用 Tasks API + 正则提取自定义标记（RX 对象）
│   │   ├── process/                   # 业务处理模块
│   │   │   ├── common-process.js      # 日期工具集（DateUtils）
│   │   │   ├── filter-task-process.js # 通用任务筛选（日期/状态/标记）
│   │   │   ├── calcul-chart-process.js# 图表统计计算
│   │   │   ├── task-query-process.js  # Tasks API 查询封装
│   │   │   ├── organize-task-process.js # 整理箱编辑操作与快照管理
│   │   │   ├── inbox-task-process.js
│   │   │   ├── kanban-task-process.js
│   │   │   ├── matrix-task-process.js
│   │   │   ├── recurring-task-process.js
│   │   │   └── tree-task-process.js
│   │   └── write/
│   │       └── write-tasks.js         # 任务写入：文件修改、快照生成与恢复
│   ├── storages/
│   │   └── persist-storages.js         # 导航中心状态持久化（createInitialState / PersistenceManager）
│   ├── echarts/
│   │   └── echarts.js                 # ECharts 本地封装
│   └── utils/
│       └── logger.js                  # 日志工具（生产环境静默）
└── skills/                            # AI 开发引用文件（本项目知识库）
```



## 全局数据结构说明

## 常量

### 任务标记设计

#### 任务标记格式


本插件使用自定义的任务行标记语法：

执行状态 任务描述 优先级 循环周期 创建日期 计划日期 开始日期 截止日期 完成日期 取消日期 唯一ID 依赖ID 标签

各标记的 Emoji、说明和示例如下表：

| 标记     | Emoji       | 说明                   | 示例             |
| -------- | ----------- | ---------------------- | ---------------- |
| 执行状态 | 自定义      | 见下方状态映射         | `- [ ]` (未开始) |
| 任务描述 | -           | 纯文本                 | `制作初步音乐库` |
| 优先级   | 🔺⏫🔼🔽⏬       | 六个等级               | `🔺` (最高)       |
| 循环周期 | 🔁 every ... | 重复规则               | `🔁 every week`   |
| 创建日期 | ➕           | 任务创建日             | `➕ 2025-09-06`   |
| 计划日期 | ⏳           | 计划执行日             | `⏳ 2025-09-06`   |
| 开始日期 | 🛫           | 实际开始日             | `🛫 2025-09-06`   |
| 截止日期 | 📅           | 截止日                 | `📅 2026-04-06`   |
| 完成日期 | ✅           | 实际完成日             | `✅ 2023-04-17`   |
| 取消日期 | ❌           | 取消日                 | `❌ 2023-04-18`   |
| 唯一 ID  | 🆔           | 任务标识               | `🆔 dcf64c`       |
| 依赖 ID  | ⛔           | 依赖的其他任务 ID 列表 | `⛔ dcf64c,h17ye` |
| 标签     | 🏁           | 自定义标签             | `🏁 keep`         |

任务实例：

- 任务描述 🔼 🔁 every week ➕ 2025-09-06 ⏳ 2025-09-06 🛫 2025-09-06 📅 2026-04-06 ✅ 2023-04-17 🆔 dcf64c ⛔ dcf64c,h17ye 🏁 keep
- 任务描述 🔼 🔁 every week ➕ 2025-09-06 ⏳ 2025-09-06 🛫 2025-09-06 📅 2026-04-06 ❌ 2023-04-18 🆔 dcf64c ⛔ dcf64c,h17ye 🏁 keep

```yaml
算法:
  名称: 任务标记解析
  复杂度: O(n)
  伪代码: |
    1. 读取任务行文本
    2. 按固定顺序解析各标记字段
    3. 执行状态：自定义Emoji，底层保留Markdown符号
    4. 优先级：🔺=1最高, ⏫=2, 🔼=3, 🔽=4, ⏬=5, 无=6
    5. 日期约束：创建≤计划≤开始≤截止
    6. 返回结构化Task对象

关键条件:
  描述: 补全日期时需保持 创建日期 ≤ 计划日期 ≤ 开始日期 ≤ 截止日期
  相关函数:
    - validateTaskDates
    - parseTaskLine
  边界条件:
    - 日期字段缺失时留空，不触发排序
    - 格式错误的行返回null
```

#### 任务标记顺序


当需要自动重排一个任务行的所有标记时，应遵循以下顺序：

1. 执行状态 🔲|❔|⏩|✅|❎|⬛
2. 任务描述 纯文本
3. 优先级 🔺|⏫|🔼|🔽|⏬
4. 循环周期 🔁
5. 创建日期 ➕
6. 计划日期 ⏳
7. 开始日期 🛫
8. 截止日期 📅
9. 完成日期 ✅
10. 取消日期 ❌
11. 唯一 ID 🆔
12. 依赖 ID ⛔
13. 标签 🏁

同类内部排序规则：

- 执行状态：未开始 🔲 → 计划中 ❔ → 进行中 ⏩ → 已完成 ✅ → 已取消 ❎ → 非任务 ⬛
- 优先级：🔺 → ⏫ → 🔼 → 🔽 → ⏬ → 普通（无优先级）
- 循环周期：`🔁 every day` → `🔁 every week` → `🔁 every month` → `🔁 every year` → `🔁 every <n> days/weeks/months`
- 日期标记：➕ 创建日期 → ⏳ 计划日期 → 🛫 开始日期 → 📅 截止日期 → ❌ 取消日期 → ✅ 完成日期

```yaml
算法:
  名称: 任务标记自动排序
  复杂度: O(n log n)
  伪代码: |
    1. 按固定顺序重排任务行中的所有标记
    2. 同类内部排序：
       - 执行状态：🔲→❔→⏩→✅→❎→⬛
       - 优先级：🔺→⏫→🔼→🔽→⏬→普通
       - 循环周期：day→week→month→year→自定义间隔
       - 日期：创建≤计划≤开始≤截止
    3. 返回排序后的任务行

事件流:
  步骤:
    - 触发: 用户选择自动排序
      动作: 调用 autoReorderTaskLine()
      下一步: 刷新当前视图
```

### 共用颜色

- 颜色必须使用 Obsidian CSS 变量（如 `--background-primary`）。
- 深色模式自动适配，Canvas 中动态获取颜色。
- 任务卡片左侧颜色条：
  - 未开始 `#2e333b`
  - 计划中 `#4b525b`
  - 进行中 `#7fb8f0`
  - 已完成 `#47852f`
  - 已取消 `#c3393e`

## 变量

## 数据结构

{在此章节末尾嵌入状态 YAML 块，可包含多个变量}

```yaml
状态:
  - 变量:
      名称: {变量名}
      类型: {数据类型}
      默认值: {初始值}
      值列表: {若为枚举，列出所有可能值}
      描述: {变量用途和约束说明}
  - 变量:
      名称: {变量名}
      类型: {数据类型}
      默认值: {初始值}
      描述: {说明}
```



## CSCI部件

{在每个部件的自然语言描述末尾嵌入对应的 YAML 块}

## 执行概念

## 接口设计

## 接口标识与接口图

## (接口的项目唯一标识符)

{在每个接口的自然语言描述末尾嵌入接口 YAML 块}

```yaml
接口:
  名称: {接口名称}
  方向: {输入/输出/双向}
  类型: {函数调用/消息/数据流}
  数据元素:
    - 名称: {参数或字段名}
      类型: {参数类型}
      范围: {合法值域}
      精度: {数值精度}
  协议: {通信协议或约定}
  错误码:
    - {可能返回的错误码}
```

# CSCI详细设计

{在以下每个模块的自然语言描述末尾，根据需要嵌入算法、事件流、关键条件 YAML 块，可嵌入多个}



---

## 按钮面板设计

### 查询面板

- 任务时间计算方式选择按钮，计划~截止， 开始~完成两个状态切换
  - 默认，计划时间~截止时间
  - 开始时间~完成时间（缺少完成时间则使用截止时间）
- 执行查询按钮
  - 在所有任务按钮的右侧

#### 快捷查询按钮面板

从左到右依次排列

- 今天按钮，今天的00:00:00~23:59:59
- 昨天按钮，今天的前1天，前1天00:00:00~前1天的23:59:59
- 明天按钮，今天的后1天，后1天的00:00:00~后1天的23:59:59
- 本周按钮，今天所在周，本第一天00:00:00~最后一天223:59:59
- 上周按钮，今天所在周的前一周，前一周第一天00:00:00~前一周最后一天23:59:59
- 下周按钮，今天所在周的后一周，后一周第一天00:00:00~后一周最后一天23:59:59
- 本月按钮，今天所在月，本月第一天00:00:00~本月最后一天23:59:59
- 上月按钮，今天所在月的前一月，前一月第一天00:00:00~前一月最后一天23:59:59
- 下月按钮，今天所在月的后一月，后一月第一天00:00:00~后一月最后一天23:59:59
- 所有时间按钮，在所有查询到的任务中计算任务时间范围，任务标记时间最小的时间~任务标记时间最大的时间

#### 时间段查询按钮面板

年份，季度，月份，周数，周几从上到下排列

每个分组中按钮从左到右依次排列

年份

- 今年的前5年，每年一个按钮
- 今年按钮
- 今年的后五年，每年一个按钮

季度

- 第1季度按钮
- 第2季度按钮
- 第3季度按钮
- 第4季度按钮

月份

- 1月按钮
- 2月按钮
- 3月按钮
- 4月按钮
- 5月按钮
- 6月按钮
- 7月按钮
- 8月按钮
- 9月按钮
- 10月按钮
- 11月按钮
- 12月按钮

周数

- 第1周按钮
- 第2周按钮
- 第3周按钮
- 第4周按钮

周几

- 周一按钮
- 周二按钮
- 周三按钮
- 周四按钮
- 周五按钮
- 周六按钮
- 周日按钮

#### 执行状态查询按钮面板

从左到右依次排列

- 未开始按钮
- 计划中按钮
- 进行中按钮
- 已完成按钮
- 已放弃按钮

#### 任务标记查询按钮面板

从左到右依次排列

- 优先级按钮
- 循环按钮
- 创建按钮
- 计划按钮
- 开始按钮
- 截止按钮
- 完成按钮
- 取消按钮
- 标签按钮
- 唯—ID按钮
- 引用ID按钮

### 显示隐藏视图面板

从左到右依次排列

- 显示或隐藏循环任务
  - 添加一个勾选框，不勾选表示隐藏，默认不勾选
  - 不勾选，切换到视图时就隐藏循环任务
  - 切换到勾选，显示循环任务。
- 显示或隐藏已完成任务
  - 添加一个勾选框，不勾选表示隐藏，默认不勾选
  - 不勾选，切换到视图时就隐藏已完成任务
  - 切换到勾选，显示已完成任务。
- 显示或隐藏已取消任务
  - 添加一个勾选框，不勾选表示隐藏，默认不勾选
  - 不勾选，切换到视图时就隐藏已取消任务
  - 切换到勾选，显示已取消任务。

### 清除数据按钮

清除查询按钮的用户选中状态

清除缓存数据

### 排序按钮面板

从左到右依次排列

对基础任务视图的任务列表排序，单个或多个列表 ，正序逆序

- 执行状态标记
- 优先级标记
- 任务描述标记
- 循环标记
- 任务时间标记
  - 创建时间标记
  - 计划时间标记
  - 开始时间标记
  - 截止时间标记
  - 完成时间标记
  - 取消时间标记
- 标签标记
- 唯一ID标记
- 引用ID标记

### 侧边栏面板

侧边栏从上到下排列按钮，按照GTD分组

- 收集按钮
	- 任务收集箱按钮
- 整理按钮
	- 任务整理处按钮
	- 任务矩阵按钮
	- 任务看板按钮
- 回顾按钮
	- 重要任务按钮
	- 循环任务按钮
	- 今天任务按钮
	- 未来 15 天任务按钮
	- 未来任务按钮
	- 逾期任务按钮
	- 依赖任务按钮
	- 标签任务按钮
	- 所有任务时间轴按钮
	- 所有任务任务表按钮
	- 所有任务任务树按钮
	- 所有任务日历图按钮
	- 所有任务甘特图按钮
- 执行
- 统计
	- 所有任务基础统计图按钮
	- 所有任务详细统计图按钮
	- 番茄钟统计图按钮

### 隐藏和显示

- - 

### 排序选择按钮

- 默认按计划日期从近到远
- 根据指定顺序排列[任务标记顺序](####任务标记顺序)

### 任务时间切换勾选框

- 固定条件
  - 创建时间，开始时间，完成时间，计划时间，截止时间，取消时间有一个时间与日历中某天日期相同，判断为任务时间和日历时间有交集
- 切换条件
  - 添加一个勾选框，不勾选表示使用 ，默认不勾选
  - 不勾选，就使用计划时间~截止时间判断任务和日历时间有交集
  - 切换到勾选，就使用开始时间~完成时间判断任务和日历时间有交集
  - 

## 交互设计

### 按钮逻辑

#### 查询面板按钮逻辑

1. 快捷查询按钮面板
   - 默认选中本周，选中后保持高亮
   - 和时间段查询按钮面板的按钮二选一。
   - 选中的按钮加入查询条件
2. 时间段查询按钮面板
   - 默认不选中任何按钮
   - 和快捷查询按钮面板的按钮二选一。
   - 选中的按钮加入查询条件
   - 多级选择逻辑
     1. 年份按钮多选一
     2. 季度按钮多选
        1. 季度按钮选一
           1. 月份按钮多选
              1. 月份按钮选一
                 1. 周数选一
                    1. 周几选一
                       1. 不能再往下选了
                    2. 周几多选
                       1. 不能再往下选了
                 2. 周数多选
                    1. 不能再往下选了
              2. 月份按钮多选
                 1. 不能再往下选了
        2. 季度按钮选多
           1. 不能再往下选了
3. 执行状态查询按钮面板
   - 默认选中全部按钮
   - 选中的按钮加入查询条件，不选中的按钮从查询条件中排除
4. 任务标记查询按钮面板
   - 默认选中全部按钮
   - 选中的按钮加入查询条件，不选中的按钮从查询条件中排除
5. 任务时间计算方式选择按钮
   1. 默认，计划时间~截止时间
6. 执行查询按钮

#### 显示隐藏视图面板按钮逻辑

多选

#### 排序按钮面板按钮逻辑

- 点击分组按钮，展开收缩其下的按钮。
  - 默认全部展开
- 多选一

### 视图缩放

### 鼠标悬停提示

- 侧边栏按钮
  - 提示任务视图全名参考[侧边栏面板](###侧边栏面板)
- 任务行，任务线条
  - 排除任务描述
  - 每个任务标记单独一行，指定顺序如下：
    - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）
    - 优先级（🔺⏫🔼🔽⏬）
    - 🔁 循环周期（🔁 every day 🔁 every week 🔁 every month 🔁 every year）
    - ➕ 创建日期 
    - ⏳ 计划日期 
    - 🛫 开始日期
    - 📅 截止日期
    - ✅ 完成日期
    - ❌ 取消日期 
    - 🏁 keep（标签）
  - 提示框深色背景

#### 侧边栏面板按钮逻辑

多选一



## 任务视图设计

### 基础列表视图

一个基础列表视图由一个或多个任务行组成。

##### 一个任务行的任务信息显示

- 显示完整任务信息
  - 任务执行状态
  - 任务描述，在第一行
  - 所有任务标记，在第二行
    - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）优先级（🔺⏫🔼🔽⏬）🔁 循环周期（🔁 every day 🔁 every week 🔁 every month 🔁 every year）➕ 创建日期 ⏳ 计划日期 🛫 开始日期📅 截止日期✅ 完成日期❌ 取消日期 🏁 keep（标签）
- 排序
  - 默认按计划日期从近到远

##### 交互

- 编辑
  - 点击基础列表视图中的任务后跳转到源文件任务位置

### 基础卡片视图

一个基础片视图由一个或多个基础类表视图组成。

###### UI设计

边框

### 任务收集箱

在 DataviewJS 中嵌入 Tasks 查询结果，实现任务收集箱视图。从指定路径 `pages/A 系统/A 任务系统` 下文件名以“任务”结尾的文件中，查询执行状态为“未完成”（`- [ ]`）和“计划中”（`- [?]`）的任务，按状态名称分组后，每组内按优先级排序。

```yaml
算法:
  名称: 任务收集箱查询与渲染
  复杂度: O(n log n)
  伪代码: |
    1. 通过 DataviewJS 读取指定路径下匹配文件模式的所有页面
    2. 嵌入 Tasks 查询，过滤出 - [ ] 和 - [?] 状态的任务
    3. 按状态名称分组：未完成、计划中
    4. 每组内按优先级数值升序排序
    5. 渲染分组后的任务卡片列表

事件流:
  步骤:
    - 触发: 用户切换到收集箱视图
      动作: 调用 DataviewJS + Tasks 查询
      下一步: 渲染分组后的任务列表

关键条件:
  描述: 只查询 - [ ] 和 - [?] 两种执行状态的任务
  相关函数:
    - 任务收集箱渲染函数
  边界条件:
    - 空数据时显示空状态提示
```

### 任务整理处

侧重于任务标记的补全、修改、批量编辑与预览。

#### 功能

##### tasks 任务标记多级查询

###### 三级查询级联按钮设计

- 多选多个选项之间是且的关系
- 一级查询选项，任务状态和任务标记的固定组合。
  - 四个必选一
  - 启动时默认选中第一个一级查询按钮，同时初始化所有关联按钮状态。
  - 未完成&缺失必需标记
    - （未完成的任务 - [] 或计划中 - [?]）且 tasks 任务标记缺失优先级标记，创建时间标记，计划时间标记，开始时间标记，截止时间标记中任何一个或多个的任务
    - 和二、三级查询按钮显示和隐藏关联
      - 二级筛选按钮组，显示在视图中
        - 未开始 计划中。
        - 默认将未开始，计划中设置为选中状态
      - 二级筛选按钮组，隐藏在视图中
        - 进行中 已完成 已放弃。
      - 三级筛选按钮组，显示在视图中
        - 优先级 循环 创建 计划 开始 截止 完成 取消 标签 唯一 ID 引用 ID
      - 三级筛选按钮组，隐藏在视图中
  - 未完成&格式完整
    - 查询条件：（未完成的任务 - [] 或计划中 - [?]）且 tasks 任务标记不缺失优先级标记，创建时间标记，计划时间标记，开始时间标记，截止时间标记中任何一个任务
    - 和二、三级查询按钮显示和隐藏关联
      - 二级筛选按钮组，显示在视图中
        - 未开始 计划中。
        - 默认将未开始，计划中设置为选中状态
      - 二级筛选按钮组，隐藏在视图中
        - 进行中 已完成 已放弃。
      - 三级筛选按钮组，显示在视图中
        - 循环 完成 取消 标签 唯一 ID 引用 ID
      - 三级筛选按钮组，隐藏在视图中
        - 优先级 创建 计划 开始 截止
  - 已完成&缺失必需标记
    - （进行中 - [/] 或已完成的任务 - [x]）或已放弃 - [-]）且 tasks 任务标记缺失优先级标记，创建时间标记，计划时间标记，开始时间标记，截止时间标记，完成任务时间标记中任何一个或多个的任务
    - 和二、三级查询按钮显示和隐藏关联
      - 二级筛选按钮组，显示在视图中
        - 进行中 已完成 已放弃。
        - 默认将进行中 已完成 已放弃设置为选中状态
      - 二级筛选按钮组，隐藏在视图中
        - 未开始 计划中。
      - 三级筛选按钮组，显示在视图中
        - 优先级 循环 创建 计划 开始 截止 完成 取消 标签 唯一 ID 引用 ID
      - 三级筛选按钮组，隐藏在视图中
  - 已完成&格式完整
    - 查询条件：（进行中 - [/] 或已完成的任务 - [x]）或已放弃 - [-]）且 tasks 任务标记不缺失优先级标记，创建时间标记，计划时间标记，开始时间标记，截止时间标记中任何一个任务
    - 和二、三级查询按钮显示和隐藏关联
      - 二级筛选按钮组，显示在视图中
        - 进行中 已完成 已放弃。
        - 默认将进行中 已完成 已放弃设置为选中状态
      - 二级筛选按钮组，隐藏在视图中
        - 未开始 计划中。
      - 三级筛选按钮组，显示在视图中
        - 优先级 循环 完成 取消 标签 唯一 ID 引用 ID
      - 三级筛选按钮组，隐藏在视图中
        - 优先，创建，计划，开始，截止
- 二级查询选项,任务状态选项,在第一级筛选后过滤
  - 未开始 - []
  - 计划中 - [?]
  - 进行中 - [/]
  - 已完成 - [x]
  - 已放弃 - [-]
- 三级查询选项,任务标记选项，在第二级筛选后过滤
  - 包含标记和排除标记的的选中状态互斥，选中其中一个后保持选中状态并高亮，同时被纳入查询条件，另一个取消选中状态不纳入查询条件。
  - 可以鼠标点击选中状态和高亮状态的按钮取消，实现两个互斥的按钮都不被选中，都不纳入查询条件
  - 包含标记
    - 按钮上的文字说明带 emoji 符号
    - 优先级标记 创建时间标记➕
    - 开始时间标记🛫 计划时间标记⏳ 截止时间📅
    - 完成任务时间标记✅ 取消任务时间标记❌
    - 循环标记🔁 ID 标记🆔 引用 ID 标记⛔ 旗帜标记🏁
  - 排除标记
    - 按钮上的文字说明带 emoji 符号
    - 优先级标记 创建时间标记➕
    - 开始时间标记🛫 计划时间标记⏳ 截止时间📅
    - 完成任务时间标记✅ 取消任务时间标记❌
    - 循环标记🔁 ID 标记🆔 引用 ID 标记⛔ 旗帜标记🏁

###### 查询按钮设计

- 没点击查询按钮就不查询任务。
- 点击查询按钮时将一级查询按钮，二级查询按钮，三级查询按钮的查询消减综合起来，开始正式查询任务。

##### tasks 任务标记编辑单个修改

取消一级按钮后也同时取消其关联编辑按钮的选中状态

###### 标记编辑按钮的显示和隐藏

- 查询条件：未完成&缺失必需标记
  - 编辑按钮组，显示在视图中。
    - 需要补全优先级标记，创建时间标记组按钮。
    - 需补全计划时间标记，开始时间标记，截止时间标记组按钮。
    - 需要补全日期标记。
    - 需补全其他缺失的标记。
      - 循环标记🔁组按钮。
      - ID 标记🆔组按钮。
      - 引用 ID 标记⛔组按钮。
      - 旗帜标记🏁组按钮。
    - 需格式化排序。
  - 编辑按钮组，隐藏在视图中
    - 完成任务时间标记，取消任务时间标记。
- 查询条件：未完成&格式完整
  - 说明：进行中的任务，格式完整
  - 编辑按钮组，隐藏在视图中
    - 需补全其他缺失的标记。
      - 循环标记🔁组按钮。
      - ID 标记🆔组按钮。
      - 引用 ID 标记⛔组按钮。
      - 旗帜标记🏁组按钮。
    - 需格式化排序。
  - 编辑按钮组，隐藏在视图中
    - 不需要优先级，创建时间标记标记组按钮。
    - 不需要计划时间标记，开始时间标记，截止时间标记组按钮。
    - 不需要补全日期标记组按钮。
    - 不需要补全完成任务时间标记，取消任务时间标记。
- 查询条件：已完成&缺失必需标记
  - 说明：已完成的任务，格式不完整
    - 编辑按钮组，显示在视图中。
      - 需要补全优先级标记，创建时间标记组按钮显示在视图中。
      - 需补全计划时间标记，开始时间标记，截止时间标记组按钮显示在视图中。
      - 需要补全日期标记。
      - 需要补全完成任务时间标记，取消任务时间标记组按钮显示在视图中。
      - 需补全其他缺失的标记。
        - 循环标记🔁组按钮显示在视图中。
        - ID 标记🆔组按钮显示在视图中。
        - 引用 ID 标记⛔组按钮显示在视图中。
        - 旗帜标记🏁组按钮显示在视图中。
      - 需格式化排序。
    - 编辑按钮组，隐藏在视图中
- 查询条件：已完成&格式完整
  - 说明：已完成的任务，格式完整，但顺序不是指定顺序
  - 编辑按钮组，显示在视图中
    - 需要补全完成任务时间标记，取消任务时间标记组按钮显示在视图中。
    - 需补全其他缺失的标记。
      - 循环标记🔁组按钮显示在视图中。
      - ID 标记🆔组按钮显示在视图中。
      - 引用 ID 标记⛔组按钮显示在视图中。
      - 旗帜标记🏁组按钮显示在视图中。
    - 需格式化排序。
  - 编辑按钮组，隐藏在视图中
    - 不需要优先级，创建时间标记标记组按钮显示在视图中。
    - 不需要计划时间标记，开始时间标记，截止时间标记组按钮显示在视图中。
    - 不需要补全日期标记组按钮显示在视图中。

###### 标记编辑按钮的简单编辑功能

- 每点击一下编辑按钮就更新预览内容，允许多个编辑按钮同时编辑。
- 编辑目标：优先级标记，即⏬🔽🔼⏫🔺
  - 修改，支持多选和批量
    - 选一个选项
      - ⏬🔽🔼⏫🔺
    - 选择不同的图标替换原本的标记
    - 不存在就使用一个选项添加
    - 已存在就使用一个选项替换
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：循环标记🔁
  - 修改，支持多选和批量
    - 选一个选项，带标记
      - 🔁 every
      - 🔁 every day
      - 🔁 every week
      - 🔁 every month
      - 🔁 every year
    - 自定义循环规则输入
      - 除预设外，循环分组提供文本框输入自定义规则（如 every 3 days）
    - 选择不同的选项替换原本的标记
    - 不存在就使用一个选项添加
    - 已存在就使用一个选项替换
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：创建时间标记➕
  - 修改，支持多选和批量
    - 不存在就使用指定日期添加，默认今天
    - 不存在就使用指定日期替换，默认今天
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：计划时间标记⏳
  - 修改，支持多选和批量
    - 不存在就使用指定日期添加，默认今天
    - 不存在就使用指定日期替换，默认今天
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：开始时间标记🛫
  - 修改，支持多选和批量
    - 不存在就使用指定日期添加，默认今天
    - 不存在就使用指定日期替换，默认今天
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：截止时间📅
  - 修改，支持多选和批量
    - 不存在就使用指定日期添加，默认今天
    - 不存在就使用指定日期替换，默认今天
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：完成任务时间标记✅
  - 修改，支持多选和批量
    - 不存在就使用指定日期添加，默认今天
    - 不存在就使用指定日期替换，默认今天
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：取消任务时间标记❌
  - 修改，支持多选和批量
    - 不存在就使用指定日期添加，默认今天
    - 不存在就使用指定日期替换，默认今天
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：旗帜标记🏁
  - 修改，支持多选和批量
    - 选一个选项，带标记
      - 🏁 keep
      - 🏁 delete
    - 选择不同的选项替换原本的标记
    - 不存在就使用一个选项添加
    - 已存在就使用一个选项替换
    - 自定义标签值
      - 标签分组提供文本框输入
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：ID 标记 "🆔 关键字 "
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过
- 编辑目标：引用 ID 标记 "⛔ 关键字,关键字 "
  - 删除，支持多选和批量
    - 删除任务中存在的标记，不存在就跳过

###### 标记编辑按钮的复杂编辑功能

- 每点击一下编辑按钮就更新预览内容，允许多个编辑按钮同时编辑。
- 编辑目标：补全截止时间标记，开始时间标记，计划时间标记
  - 自动补全和修改时间
    - 判断是否存在任务完成时间，存在才补全和修改，不存在就跳过。
    - 开始补全前先对标记排序，保证标记顺序正确。
      - 补全时设置参数
        - 可指定任务执行时间为 n 天，即开始日期为截止日期的第前 n 天，继而计算出开始日期。
        - n 大于等于 0
    - 整体补全和修改逻辑
      - 从完成日期开始往前补全和修改，因为完成日期是最晚的日期，最早日期是创建日期。
        - 创建日期小于等计划日期，计划日期小于等于开始日期，开始日期小于等于截止日期，截止日期等于完成日期，
        - 依次寻找完成时间标记，截止时间标记，开始时间标记，计划时间标记，
        - 使用前一个标记的时间补全后一个标记
        - 判断后一个标记的后一个标记的时间是否大于后一个标记的时间。
          - 如果大于
            - 就将后一个标记的后一个标记的时间改为小于等于后一个标记的时间。
              - 如果后一个标记是开始时间
                - 根据 n 的值补全和修改开始时间
          - 如果小于等于
            - 后一个标记的后一个标记的时间不变
    - 补全和修改的可能情况
      - 不缺失时间标记
        - 判断开始时间和完成时间相隔是否 n 天
          - 不是相隔 n 天
            - 按照整体补全和修改逻辑的方法修改
          - 是相隔 n 天
            - 保持原有的标记时间不变
      - 缺失单个时间标记
        - 按照整体补全和修改逻辑的方法补全
      - 缺失多个时间标记
        - 依次使用缺失单个时间标记的方法补全，直到全部补全
- 编辑目标：任务标记按照指定顺序排序
  - 根据 tasks 任务标记格式化顺序中的指定顺序排序。

###### 标记编辑按钮布局设计

- 标记编辑按钮分组
  - 使用紧凑布局
  - 按钮的两中情况排列
    - 删除按钮 修改按钮
    - 删除按钮 修改按钮说明：修改选项按钮
  - 编辑说明，编辑 xx，带 emoji，如：编辑开始时间 🛫
  - 组合布局，开始时间标记 🛫：修改按钮 删除按钮
- 编辑操作按钮所有分组排序
  - 从上到下按照 tasks 任务标记格式化排序🔁
    - 优先级标记 🔺：删除按钮 修改按钮 选择优先级
    - 优先级标记 🔁：删除按钮 修改按钮 选择优先级
    - 创建时间标记 ➕：删除按钮 修改按钮 选择时间
      - ……
- 标记编辑按钮逻辑设计
  - 选择某个编辑分组中按钮
    - 高亮按钮
    - 可同步预览此编辑修改结果
  - 选择多个编辑分组中按钮
    - 高亮按钮
    - 可同步预览多个编辑修改结果
  - 取消选择某个编辑分组中按钮
    - 取消高亮按钮
    - 取消预览此编辑修改结果
  - 取消多个编辑分组中按钮
    - 取消高亮按钮
    - 取消预览多个编辑修改结果
  - 编辑中的修改功能通过按钮选中参数
    - 点击修改按钮切换到选中状态，右侧显示出一排按钮，每个按钮代表一个选项的修改，预览逻辑和其他编辑按钮一样
    - 每个有多个选项的按钮都这样实现
    - 再次点击修改按钮切换到取消选中状态，隐藏所有选项按钮
  - 选择自动补全时间按钮
    - 右侧可指定参数
  - 选择自动格式化按钮
    - 按照 tasks 任务标记格式化排序进行排序
  - 选择多个编辑分组中按钮
    - 高亮按钮
    - 可同步预览多个编辑修改结果
      - 按照 tasks 任务标记格式化排序进行排序
  - 取消选择某个编辑分组中按钮
    - 取消高亮按钮
    - 取消预览此编辑修改结果
      - 按照 tasks 任务标记格式化排序进行排序
  - 取消多个编辑分组中按钮
    - 取消高亮按钮
    - 取消预览多个编辑修改结果
      - 按照 tasks 任务标记格式化排序进行排序
  - 编辑分组中按钮多选一
    - 存在多个子选项的编辑按钮
      - 从能下拉选中的选择组件选一个预设值
      - 从能下拉选中的 选择组件选一个日期
  - 选择补全时间按钮
    - 右侧可指定参数
    - 按照 tasks 任务标记格式化排序进行排序
  - 选择自动排序按钮
    - 按照 tasks 任务标记格式化排序进行排序

#### tasks 任务标记编辑批量修改

- 数据来源
  - 启动时不加载任何任务
  - 启动后
    - 点击查询从源文件查询符合条件的任务
- 数据呈现
  - 加载的任务呈现在任务列表中
  - 任务列表设计
    - 简洁模式：任务行，每行显示复选框和任务文本，点击即可跳转到文档对应行并进入编辑模式（光标定位准确）。
    - 排序规则：
      - 计划日期（近，远，可切换）
      - 缺失标记数量排序（多，少，可切换）
      - 优先级排序（高，低，可切换）
      - 任务标记级排序是否符合指定顺序（合，不合）
    - 分页机制：任务总数超过 100 条时自动分页（每页 50 条），提供上一页/下一页按钮，避免渲染卡顿。
    - 预览修改：在被勾选的任务行中的原始文本 (复选框和任务文本）下方显示修改预览。
- 数据筛选
  - 通过勾选任务行前的复选框对任务列表中希望修改的任务进行指定
    - 默认使用全选勾选任务列表的当前页的任务行。
      - 在勾选后就提供默认预览，与任务列表的当前页的被勾选任务行为原始文本一样
    - 全选/全不选
      - 点击全不选，去除任务列表的当前页的任务行勾选，按钮切换为全选
      - 点击全选，再次全选任务列表的当前页的任务行，按钮切换为全不选
      - 全选/全不选按钮放在任务列表上方
    - 手动单选和取消单选
  - " 顺序不符合 " 筛选按钮
    - 在任务列表中添加一个筛选项，筛选出任务标记不符合指定排序的任务行。
- 数据修改
  - 以任务列表的当前页的被勾选任务行为原始文本（来自源文件）。
  - 点击编辑按钮进行当前页的被勾选任务行为原始文本修改。
    - 参考：标记编辑按钮的编辑功能
      - 注意，编辑按钮的编辑目标，都是预览 tasks 的任务标记部分，不改变任务描述内容，不改变行首的格式，如：- []
    - 参考：标记编辑按钮的复杂编辑功能
      - 注意，编辑按钮的编辑目标，都是预览 tasks 的任务标记部分，不改变任务描述内容，不改变行首的格式，如：- []
- 修改预览
  - 在被勾选的任务行中的原始文本 (复选框和任务文本）下方显示修改预览。
    - 在初次勾选后
      - 提供默认预览，与任务列表的当前页的被勾选任务行为原始文本一样
    - 非初次勾选
      - 勾选一个就同时预览一个
      - 勾选多个行就同时预览多个
      - 没有被勾选的任务行不加入修改和预览
      - 被勾选又被取消的的任务行，隐藏预览内容
    - 预览内容出现后就一直在被勾选任务行下方显示，只是在不断被编辑按钮编辑，同时内容的更新变化。
  - 预览累积：每次点击标记编辑按钮时按顺序在预览中更新，后一个更新操作基于前一个更新操作的结果，确保连续编辑正确
    - 在第一次出现预览内容后，所有编辑按钮的修改的参考对象都变成预览内容，而不是原始文本。举例：
      - 假设原始任务中没有优先级，
      - 第一次编辑通过编辑按钮添加了优先级并出现了预览内容，
      - 第二次编辑通过编辑按钮删除优先级，
        - 应该以第一次编辑后的预览内容为参考，从第一次编辑后的预览内容中删除优先级，而不是第一次编辑前原始文本进行删除操作。
        - 如果参考原始文本，原始文本中没有优先级，当点击删除时按钮时，按照原本逻辑应该跳过，那么就无法删除预览内容中的优先级，就无法多次编辑。
- 清空预览
  - 清控任务列表的当前页的全部被勾选任务行的预览内容
- 确定修改
  - 任务列表变化
  - 任务行 " 确定 " 按钮
    - 当任务行出现预览内容时 (前面默认勾选时)，同时出现一个任务行 " 确定 " 按钮
    - 点击任务行 " 确定 " 按钮
      - 只修改任务行状态，不进行列表整体刷新
        - 如果刷新，只刷新列表中最近一次任务行 " 确定 " 按钮点击修改的任务列表任务行
      - 源文件文本替换
        - 将任务列表的当前页的单个被勾选任务行的预览内容，替代源文件中的相应内容
        - 将任务列表的当前页的单个不被勾选任务行的预览内容，忽略，即不替代源文件中的相应内容
        - 将单行修改改为纯内存操作（写入文件，Dataview 会在检测到文件修改后自动刷新视图，导致脚本重新执行，状态丢失，任务列表重新渲染。）
          - 仅在界面上切换任务状态，并提供一个 " 保存所有修改 " 按钮，点击时批量将当前所有已修改任务写入文件。
          - 这样单行 " 确定 " 不会引发文件变更，视图保持完全稳定；
          - 而批量保存虽然会触发刷新，但那是用户主动触发的全局操作，体验可接受。
      - 状态切换
        - 去掉勾选状态，不能再被勾选，用 emoji 符号✔ 替代复选框的选中
          - 除非点击任务行 " 撤回 " 按钮
        - 预览内容说明文字内容 "📝 预览:" 改为 "📝 已修改:"
        - 隐藏任务列表当前页中被修改任务行 " 确定 " 按钮
        - 显示任务列表当前页中被修改任务行 " 撤回 " 按钮
      - 状态保留
        - 任务列表当前页中排序选项不变或恢复
        - 任务列表当前页中被修改的任务行的在排序选项的排序位置不变或恢复。
        - 任务列表当前页中被修改的任务行显示的预览内容不变或恢复
    - 允许连续的单独修改和撤回
  - " 确定修改 " 按钮
    - 点击 " 确定修改 " 按钮
      - 只修改任务行状态，不进行列表整体刷新
        - 如果刷新，只刷新列表中最近一次点击 " 确定修改 " 按钮修改的任务列表任务行
      - 源文件文本替换
        - 标记哪些任务行在确认修改时被写入了源文件，存放起来。
        - 将任务列表的当前页的全部被勾选任务行的预览内容，替代掉源文件中的相应内容
        - 将任务列表的全部不被勾选任务行的预览内容，忽略，即不替代源文件中的相应内容
        - 已经被单独修改且没有单独撤回的任务行，不重复修改，忽略，即不替代源文件中的相应内容。
      - 状态切换
        - 去掉勾选状态，不能再被勾选，用 emoji 符号✔ 替代复选框的选中
          - 除非点击任务行 " 撤回 " 按钮
        - 预览内容说明文字内容 "📝 预览:" 改为 "📝 已修改:"
        - 隐藏任务列表当前页中被修改任务行 " 确定 " 按钮
        - 显示任务列表当前页中被修改任务行 " 撤回 " 按钮
      - 状态恢复
        - 任务列表当前页中排序选项恢复
        - 任务列表当前页中被修改的任务行的在排序选项的排序位置恢复。
        - 任务列表当前页中被修改的任务行显示的预览内容恢复
- 撤回修改
  - 历史快照与全局撤回
    - 允许使用独立文件存储信息，确保历史快照信息不丢失。
      - 任务务文件检查后再切换回来，obsidian 的代码块重新加载，`localStorage` 或文件存储信息保证仍能通过历史快照恢复上一次的批量修改
      - 应用批量修改后，dataview 会自动更新，`localStorage` 或文件存储信息保证仍能通过历史快照恢复上一次的批量修改
    - 记录哪些任务行在点击确认修改时批量修改了源文件，存入历史快照，以便在全局刷新后能实现批量正确还原。
      - 需要保存被修改任务行和存入历史快照任务行的一一对应关系。
      - 需要保存被修改任务的原始文本
      - 需要保存被修改源文件路径和任务行光标位置（参考批量修改和任务行跳转部分的设计）
      - 记录上几次的批量修改后的任务快照，合理限制保存快照的数量。
    - 在点击 " 确定修改 " 按钮后更新历史快照的数据。
      - 点击查询按钮局刷新后，`localStorage` 或文件存储信息保证仍能通过历史快照恢复上一次的批量修改
  - 任务行 " 撤回 " 按钮
    - 当任务行出现 "📝 已修改:"，同时出现一个任务行 " 撤回 " 按钮
    - 点击任务行撤回按钮后
      - 不进行列表刷新，只修改状态
        - 如果刷新，只刷新当前页修改的任务列表任务行
      - 撤回修改
        - 将源文件中文本的相应内容，恢复为任务列表的当前页的单个被修改任务行的原始文本
        - 将单行撤回改为纯内存操作（写入文件，Dataview 会在检测到文件修改后自动刷新视图，导致脚本重新执行，状态丢失，任务列表重新渲染。）
      - 状态切换
        - 将被撤回修改的任务行状态恢复为撤回前的勾选的状态
        - 将被撤回修改的任务行恢复预览内容说明文字内容，"📝 已修改:" 恢复为 "📝 预览:"
        - 隐藏任务列表当前页中被修改任务行 " 撤回 " 按钮
        - 显示任务列表当前页中被修改任务行 " 确定 " 按钮
  - " 撤回修改 " 按钮
    - 点击 " 快照撤回 " 按钮
      - 不进行列表刷新，只修改状态
        - 如果刷新，只刷新当前页修改的任务列表任务行
      - 撤回修改
        - 将源文件中文本的相应内容，替换为历史快照的数据的任务行
        - 需要找出被修改任务行和被存放任务行的一一对应关系，可能会跨越一级查询条件，因为再次查询时可能会进入其他一级查询条件。
      - 状态切换
        - 隐藏任务列表当前页中被修改任务行 " 撤回 " 按钮
        - 显示任务列表当前页中被修改任务行 " 确定 " 按钮
      - 状态恢复
        - 将被撤回修改的任务行状态恢复为撤回前的勾选的状态
        - 将被撤回修改的任务行恢复预览内容说明文字内容，"📝 已修改:" 恢复为 "📝 预览:"
      - 快操作方式
        - 改为按钮 + 快照列表选择的方式，默认选中最近快照。

#### UI 设计

##### 全局布局

- 查询按钮部分
- 编辑按钮部分
  - 删除按钮 修改（文字提示）+ 相应的任务标记选项或日期选择
  - 补全时间 修改按钮 + 天数文本框
  - 自动排序 修改按钮
  - 清空预览 确定修改快照撤回
- 任务列表部分
  - 排序按钮在任务列表上方
  - " 顺序不符合 " 筛选按钮在任务列表上方
- 任务列表预览部分

##### 操作逻辑

- 点击一级筛选查询按钮
- 点击二级查询按钮，可跳过
- 点击三级查询按钮，可跳过
- 点击 " 查询按钮 "
- 翻页，可跳过
- 勾选任务行
  - 全选从
  - 全不选
  - 手动单选，多选，可跳过
- 点击编辑按钮
- 确定修改
- 撤回修改，可跳过

##### 状态恢复

- 允许使用独立文件存储信息，确保视图状态信息不丢失。
- 务文件检查后再切换回来，obsidian 的代码块重新加载，`localStorage` 或文件存储信息保证仍能通过记录恢复上一次的视图状态
- 应用批量修改后，dataview 会自动更新，`localStorage` 或文件存储信息保证仍能通过记录恢复上一次的视图状态
- 视图状态恢复
  - 记录上一次点击查询按钮后的视图状态。
    - 一级查询，二级查询，三级查询，查询按钮，等。在点击查询按钮全局刷新后，仍能通过记录恢复上一次的视图状态
  - 记录上一次确认修改前的视图状态
    - 包括排序按钮，标记顺序异常，全选按钮，等。在成功修改源文件任务列表刷新后，使任务列表恢复点击确认修改按钮前的视图状态。
  - 记录上一次点击确认修改按钮后的列表状态
    - 当点击确认修改按钮后，应该保存此时的视图状态。任务行勾选状态，任务行已修改状态，等。在成功修改源文件任务列表刷新后，使任务列表恢复点击确认修改按钮后的视图状态。

### 任务矩阵

使用 Tasks API 查询 + JS 分拣进行二次过滤，用 DataviewJS + tasks 重构视图。查询未开始、计划中、进行中的任务，按四象限分类展示：

- 第 2 象限（紧急与重要）：🔺（最高优先级）
- 第 1 象限（不紧急且重要）：⏫（高优先级）
- 第 3 象限（紧急且不重要）：🔼（中优先级）
- 第 4 象限（不紧急也不重要）：🔽⏬ 及无优先级

指定路径 `pages/A 系统/A 任务系统`，指定文件名包含“任务”。

```yaml
算法:
  名称: 任务矩阵四象限分拣
  复杂度: O(n)
  伪代码: |
    1. 通过 Tasks API 查询指定路径下的所有任务
    2. JS 二次过滤：只保留未开始、计划中、进行中状态
    3. 按优先级分拣到四个象限
    4. 渲染四象限矩阵视图

事件流:
  步骤:
    - 触发: 用户切换到矩阵视图
      动作: 调用 Tasks API 查询并执行 JS 二次过滤
      下一步: 渲染四象限矩阵

关键条件:
  描述: 优先级映射规则 - 🔺=第2象限, ⏫=第1象限, 🔼=第3象限, 🔽⏬无=第4象限
  相关函数:
    - 任务矩阵分拣函数
```

### 任务看板

使用 Tasks 插件新版 API 直接导出 `getTasks` 方法。Tasks API 查询 + JS 分拣进行二次过滤，用 DataviewJS + tasks 重构视图。查询未完成、计划中、进行中的任务，分横向三列展示，紧凑布局，根据页面自动调整宽度。指定路径 `pages/A 系统/A 任务系统`，指定文件名包含“任务”。

```yaml
算法:
  名称: 任务看板三列分拣
  复杂度: O(n)
  伪代码: |
    1. 通过 Tasks API 的 getTasks 方法获取任务列表
    2. JS 二次过滤：只保留未完成、计划中、进行中状态
    3. 按执行状态分三列渲染
    4. 紧凑布局，响应式调整列宽

事件流:
  步骤:
    - 触发: 用户切换到看板视图
      动作: 调用 Tasks API 查询并执行 JS 二次过滤
      下一步: 渲染三列看板

关键条件:
  描述: 分横向三列，分别展示未完成、计划中、进行中的任务
  相关函数:
    - 任务看板分拣函数
```

### 今天任务

查询任务时间和今天相关的任务。以今天为起点，支持查看昨天、明天、本周、本月、本年及自定义时间段。指定路径 `pages/A 系统/A 任务系统`，指定文件名包含“任务”。按执行状态分组，循环任务单独分组。每个分组内先按计划时间从远到近排序，再按优先级从高到低排序。只要以下任一日期为今天，即视为相关：创建日期、计划日期、开始日期、截止日期、完成日期、取消日期；或计划-截止、开始-完成、开始-取消时间段包含今天。

```yaml
算法:
  名称: 今天任务查询与排序
  复杂度: O(n log n)
  伪代码: |
    1. 查询指定路径下所有任务
    2. 筛选与今天相关的任务（日期匹配或时间段包含）
    3. 按执行状态分组，循环任务单独分组
    4. 每组内先按计划时间从远到近排序，再按优先级从高到低排序
    5. 渲染任务列表

事件流:
  步骤:
    - 触发: 用户切换到今天任务视图
      动作: 执行时间过滤和分组排序
      下一步: 渲染分组后的任务列表

关键条件:
  描述: 日期匹配规则 - 单日期等于今天，或时间段包含今天
  相关函数:
    - 今天任务过滤函数
  边界条件:
    - 无匹配任务时显示空状态
```

### 循环任务

查询所有循环任务。指定路径 `pages/A 系统/A 任务系统`，指定文件名包含“任务”。按优先级分组，再按循环周期分组，最后按文件名分组。循环周期按时间长短排序，短周期在前。

```yaml
算法:
  名称: 循环任务分组排序
  复杂度: O(n log n)
  伪代码: |
    1. 查询指定路径下所有包含循环标记的任务
    2. 按优先级分组
    3. 每组内按循环周期分组（day→week→month→year→自定义间隔）
    4. 各周期组内按文件名分组
    5. 渲染分组结果

事件流:
  步骤:
    - 触发: 用户切换到循环任务视图
      动作: 查询循环任务并执行三级分组
      下一步: 渲染分组列表

关键条件:
  描述: 循环周期排序 - day < week < month < year < 自定义间隔
  相关函数:
    - 循环任务分组函数
```

### 未来 15 天任务

查询未开始、计划中、进行中的任务。查询任务时间与今天之后 15 天相关的任务。指定路径 `pages/A 系统/A 任务系统`，指定文件名包含“任务”。按执行状态分组，循环任务单独分组。每个分组内先按计划时间从远到近排序，再按优先级从高到低排序。日期匹配规则：任一日期在今天之后 15 天内，或计划-截止、开始-完成、开始-取消时间段与今天之后 15 天内有交集。

```yaml
算法:
  名称: 未来15天任务查询与排序
  复杂度: O(n log n)
  伪代码: |
    1. 查询指定路径下未开始、计划中、进行中的任务
    2. 筛选与今天之后15天相关的任务
    3. 按执行状态分组，循环任务单独分组
    4. 每组内先按计划时间排序，再按优先级排序
    5. 渲染任务列表

事件流:
  步骤:
    - 触发: 用户切换到未来15天视图
      动作: 执行时间段过滤和分组排序
      下一步: 渲染分组后的任务列表

关键条件:
  描述: 日期匹配规则 - 单日期在范围内，或时间段与范围内有交集
  相关函数:
    - 未来15天任务过滤函数
```

### 逾期任务

查询未开始、计划中、进行中的任务。查询截止日期在今天之前且未完成的任务。指定路径 `pages/A 系统/A 任务系统`，指定文件名包含“任务”。按执行状态分组，循环任务单独分组。每个分组内先按截止时间从近到远排序，再按优先级从高到低排序。

```yaml
算法:
  名称: 逾期任务查询与排序
  复杂度: O(n log n)
  伪代码: |
    1. 查询指定路径下未开始、计划中、进行中的任务
    2. 筛选截止日期小于今天且状态未完成的任务
    3. 按执行状态分组，循环任务单独分组
    4. 每组内先按截止时间排序，再按优先级排序
    5. 渲染任务列表

事件流:
  步骤:
    - 触发: 用户切换到逾期任务视图
      动作: 执行逾期过滤和分组排序
      下一步: 渲染分组后的任务列表

关键条件:
  描述: 截止日期 < 今天 且 状态 ≠ 已完成/已取消
  相关函数:
    - 逾期任务过滤函数
```

### 重要任务

查询未开始、计划中、进行中的任务。查询优先级为 🔺、⏫、🔼 的任务。指定路径 `pages/A 系统/A 任务系统`，指定文件名包含“任务”。按任务优先级分组升序，再按状态名称分组排序（未开始→计划中→进行中→已放弃→已完成→非任务），最后按截止时间从小到大排列。

```yaml
算法:
  名称: 重要任务查询与三级排序
  复杂度: O(n log n)
  伪代码: |
    1. 查询指定路径下优先级为🔺、⏫、🔼的未开始/计划中/进行中任务
    2. 按优先级分组升序
    3. 每组内按状态名称排序（未开始→计划中→进行中→已放弃→已完成→非任务）
    4. 各状态组内按截止时间升序
    5. 渲染任务列表

事件流:
  步骤:
    - 触发: 用户切换到重要任务视图
      动作: 执行优先级过滤和三级排序
      下一步: 渲染分组后的任务列表

关键条件:
  描述: 只查询🔺、⏫、🔼三种优先级
  相关函数:
    - 重要任务过滤函数
```

### 所有任务时间轴

查询未开始、计划中、进行中的任务。指定路径 `pages/A 系统/A 任务系统`，指定文件名包含“任务”。按截止时间分组升序，再按状态名称分组排序（未开始→计划中→进行中→已放弃→已完成→非任务），最后按优先级排序。

```yaml
算法:
  名称: 任务时间轴查询与排序
  复杂度: O(n log n)
  伪代码: |
    1. 查询指定路径下未开始、计划中、进行中的任务
    2. 按截止时间分组升序
    3. 每组内按状态名称排序
    4. 各状态组内按优先级升序
    5. 渲染时间轴视图

事件流:
  步骤:
    - 触发: 用户切换到时间轴视图
      动作: 执行三级排序
      下一步: 渲染时间轴

关键条件:
  描述: 按截止时间→状态→优先级三级排序
  相关函数:
    - 任务时间轴排序函数
```

### 所有任务表

查询所有任务。指定路径 `pages/A 系统/A 任务系统`，指定文件名以“任务”结尾。按状态名称分组排序（未开始→计划中→进行中→已放弃→已完成→非任务），再按计划时间分组，最后按优先级分组。

```yaml
算法:
  名称: 所有任务表查询与三级分组
  复杂度: O(n log n)
  伪代码: |
    1. 查询指定路径下所有任务
    2. 按状态名称分组排序
    3. 每组内按计划时间分组
    4. 各时间组内按优先级分组
    5. 渲染任务表

事件流:
  步骤:
    - 触发: 用户切换到所有任务表视图
      动作: 执行三级分组排序
      下一步: 渲染完整任务表

关键条件:
  描述: 状态排序：未开始→计划中→进行中→已放弃→已完成→非任务
  相关函数:
    - 所有任务表分组函数
```

### 所有任务树

#### 信息

##### 信息源

###### 查询路径

- 指定路径 pages/A 系统/A 任务系统
- 指定文件“*.任务”

###### 排除文件

- 排除无任务格式元数据的文件（任务名称, 计划时间, 截止时间, 开始时间, 完成时间, 执行状态, 优先级, 父任务） 优先使用 YAML 的 父任务 字段（子文件声明父文件）。

#### 任务系统文件结构如下

- 任务文件夹（任务文件夹与任务文件一一对应，名称相同）
- 任务文件（一个任务文件也表示一个任务，任务文件之间有父子关系）
  - 任务文件夹
  - 任务文件，任务
    - 任务列表，任务
      - 任务列表子列表，子任务

#### 父子关系的引用格式

- 子任务文件中记录了父任务文件名
  - 任务文件的 yaml 中 ，" 父任务 " 属性对应内容中记录了父任务文件名，实例，父任务: 个人任务管理任务，
- 父任务文件中记录了子任务文件Wiki 链接
  - 在父文件中有对应的任务文件的 Wiki 链接引用
- 父子文物文件之间引用的全部情况
  - Wiki 链接和元属性都不缺失且对应
  - 元属性缺和 Wiki 链接都缺失
  - Wiki 链接缺失元属性不缺失
  - 元属性缺失 Wiki 链接不缺失
  - Wiki 链接和元属性都不缺失

##### 功能

使用 dataview 制作任务树视图再查询文件内的任务

###### 创建任务树视图

- 同时获取子任务中的 " 父任务 " 元属性，以及父文件 Wiki 链接
- 父子文物文件之间引用的全部情况处理
  - Wiki 链接和元属性都不缺失且对应
    - 优先取元属性。
  - 元属性缺和 Wiki 链接都缺失
    - 忽略
  - Wiki 链接缺失元属性不缺失
    - 取元属性
  - 元属性缺失 Wiki 链接不缺失
    - 取 Wiki 链接
  - Wiki 链接和元属性都不缺失
    - Wiki 链接和元属性都不缺失不相同
      - 取父文件 Wiki 链接，将链接指向的有效任务文件视为子文件，自动建立父子关系。
    - Wiki 链接和元属性都不缺失且相同
      - 取元属性，对应的任务文件视为父文件，自动建立父子关系。

#### 父任务中子任务任务数量统计

- 子任务文件数量统计，计入任务数
- 任务项列表计入任务数

#### 父子关系是否完整检测

- " 父任务 " 和 obsidian 链接两者不对应或者缺失的情况提示
- 子任务文件元属性和父任务文件链接引用不对应的应该在子任务中提示 " 元属性不对应 "。
- 子任务文件元属性和父任务文件链接引用缺失文件元属性的应该在子任务中提示 " 缺失元属性 "。
- 子任务文件元属性和父任务文件链接引用缺失文件链接的应该在父任务中提示 " 缺失链接 "。
- 子任务文件元属性和父任务文件链接引用都缺失应该在父任务中提示 " 缺失链接 "，应该在子任务中提示 " 缺失元属性 "。

##### UI设计

###### 任务树窗口视图

- 任务树窗口长度
  - 默认长为 20 行文字
- 任务树窗口宽度
  - 默认宽为固定宽度
  - 点击右侧边缘鼠标拉伸窗口

##### 任务树视图

###### 树形视图

- 根节点
  - 任务所在的文件夹，如：任务系统文件夹
- 顶层节点
  - 从任务系统文件夹的下一级子文件夹作为顶层节点
- 任务行嵌套显示
  - 缩进表示任务分解的层级
- 底层节点
  - 最底层的任务使用简单列表显示
  - 单个任务行不换行
  - 单个任务行显示，任务执行状态emoji+任务描述
  - 点击排序按钮排序，各排序互斥
    - 执行状态（正逆序），未开始 → 计划中 → 进行中 → 已放弃 → 已完成 → 非任务
    - 优先级（正逆序），0=🔺, 1=⏫, 2=🔼, 3=🔽, 4=⏬, none=无
    - 时间（正逆序），创建时间 → 开始时间 → 计划时间 → 截止时间 → 取消时间 → 完成时间
- 字体
  - 所有任务行字体和 obsidian 当前字体保持一致
- 任务进度条
  - 有子项的任务标题右侧显示任务进度条
    - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）
    - 颜色，每种状态用不同颜色,参考[共用颜色](###共用颜色)
    - 颜色段占比，每种状态占一段根据数量占比
    - 颜色段顺序，从左到右对应执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）
    - 右侧显示完成百分比

##### 任务树交互

- 隐藏/显示文件夹按钮，默认隐藏
- 折叠展开
    - 含有子项的父项任可折叠展开所有子项
- 显示和隐藏
  - 隐藏和显示：任务行
    - 超出窗口长度的任务行自动隐藏
    - 超出窗口宽度的任务行自动隐藏超出部分
  - 隐藏和显示：文件夹节点
    - 添加一个勾选框，不勾选表示隐藏，默认不勾选
    - 不勾选，切换到视图时就隐藏文件夹节点
    - 切换到勾选状态，显示文件夹节点。

#### 配置状态持久化

被用户设置的非默认状态勾选框，按钮状态保持在硬盘中，下次切换到视图时还原配置。

### 所有任务日历图

#### 信息

##### 信息源

###### 查询路径

- 指定路径 pages/A 系统/A 任务系统
- 指定文件“*.任务”

#### 视图共用

##### 动态调整

- 日历视图随页面宽度调整，格子宽度也做相应的调整
- 日历日，周，月，季，年视图根据查询结果动态渲染
  - 查询日期范围内有查询结果的才进行视图渲染，
    - 跨单个日，周，月，季，年视图就渲染多个日，周，月，季，年视图，覆盖查询时间范围。
  - 查询日期范围内有没有任何一个查询结果的就不渲染，只在标题说明空白原因，
    - 如：查询时间内的某周，月，季，年无日期相关的任务，就只显示小标题，然后提示相关无任务
  - 查询所有任务后，找到对应的最小日期和最大日期，据此生成对应的周，月，季，年任务视图，
    - 时间段可能跨年，年视图中应该有多年，季视图中应该有多季，月视图中应该有多月，周视图中应该有多周视图
    - 根据判断交集时选择的开始时间到完成时间或者计划时间到截止时间判断
- 显示视图标题
  - 视图总标题
    - 根据查询条件拼接，时间查询条件 + 执行状态查询条件 + 任务标记查询条件。举例：2026 年 第 1 季度 3 月 第 2 周 周一 xxxx-xx-xx~xxxx-xx-xx 未开始，计划中，进行中 包含：优先级 排除：循环
    - 周数是相对于本月的周
  - 视图小标题
    - 单日，周，月，季，年显示各自的标题，日期或日期范围
    - 周数是相对于本年的周

##### 简单视图

- 无背景色填充，使用 obsidian 主题页面背景色
- 所有任务行字体和 obsidian 当前字体保持一致
- 周数从左到右排列，和正常日历的样式一样
- 今天的卡片用框标记
- 每天一个基础卡片视图，显示所有和这一天有交集的任务
- 默认排序从上到下按日期从近到远排序

##### 简单交互

- 鼠标悬停在任务行或任务线条提示完整任务信息
- 周，月，季视图中点击任务
  - 跳转到日视图中
  - 显示同一日期的基础列表视图，其他日期的格子隐藏
  - 聚焦格子中的相同任务
  - 点击日按钮显示全部日格子。

- 同周，月，季视图鼠标悬停在任务描述或任务线条上提示完整任务信息,参考[鼠标悬停提示](###鼠标悬停提示)

##### 日历格子任务视图

- 周，月，季，年视图
- 筛选日历日期和任务时间有交集的任务，根据任务时间切换勾选框状态
- 日历格子视图
  - 默认格子的长为 10 行文字
    - 超出格子长度的任务隐藏（在视图中隐藏，而不是藏在单独的视图中），任务线条隐藏
    - 提示剩余任务数
  - 默认格子的宽为页宽等分，不超出页面的可视范围
  - 格子视图面积调整
    - 点击日期格子后，格子从默认大小扩大面积，增加本格子宽度和高度，收缩缩右侧和下侧格子，
      - 显示没有在格子视图中显示的任务描述，且字体样式一样
      - 显示没有在格子视图中显示的任务线条
    - 再次点击日期格子后，格恢复默认大小面积，恢复其他格子面积
- 日历格子内任务行显示
  - 周，月，季视图任务行不换行显示
  - 任务行超出格子视图的隐藏。
  - 周，月，季视图在与任务最小和最大任务标记日期相同的格子中分别显示任务执行状态 + 任务描述
    - 多个周，月，季，年同存的视图时，只在首和尾显示一次任务执行状态 + 任务描述
      - 比如一个任务跨 2 周，计划时间，开始时间在第一周，截止时间，完成时间在第二周，显示时，在第一周计划时间或开始时间显示一次任务执行状态 + 任务描述，在第 2 周截止时间或完成时间显示一次任务执行状态 + 任务描述，而不是在每周开始和结束时都显示一次任务
    - 根据判断交集时选择的开始时间到完成时间或者计划时间到截止时间判断
- 日历格子内任务线条显示
  - 任务线条超出格子视图的隐藏。
  - 周，月，季视图中，每个任务最小和最大任务标记日期中间的时间段用不同颜色的线条表示时间过渡
    - 排除今天，今天显示任务执行状态 + 任务描述
    - 时间段，最小时间的下一天到最大时间的前一天
    - 线条根据执行状态自动选择颜色,参考[共用颜色](###共用颜色)
  - 任务行始终和任务过度线条高度对齐。
    - 如：✅ 制作初步音乐库和它的过渡线条要一样高。
    - 同一任务的线条高度一致，保证对齐
      - 当任务线条左侧出现已完成任务时，任务线条可能会占据已完成任务空出的位置，会错位。应该继续留空，并在后面的日期中保持留空。
  - 任务线条宽度，和任务行的文字高度一致，保证对齐
  - 任务线条长度，和任务行的任务时间一致，保证对齐
- 日历格子内任务行排序
  - 查询范围内的所有任务有个默认顺序按照
  - 在和格子内日期有交集的任务按照默认顺序排序
  - 在和格子内日期有交集的任务线条按照默认顺序排序
  - 和格子内日期无交集的出现的任务不参与排序

#### 日视图UI

- 默认显示所有查询到的任务列表
- 单日视图
  - 和这天有交集的所有任务
  - 参考[基础列表视图](####基础列表视图)
- 多日视图
  - 参考[基础列卡片图](####基础卡片视图)

#### 周视图

- 单周视图
  - 单日
    - 参考[基础列表视图](####基础列表视图)
  - 多日
    - 参考[基础列卡片图](####基础卡片视图)
  - 基础卡片视图从左到右排序，左侧为更小的日期。
- 多周视图
  - 多个 1 周视图从上到下排列

#### 月视图UI

- 单月视图
  -  4-5 个单周视图从上到下排列
- 多月视图
  - 多个单月视图从上到下排列

#### 季视图UI

- 单季视图
  -  3 个月日历视图从上到下排列
- 多季视图
  - 多个单季视图从上到下排列

#### 年视图UI

- 单年视图
  - 每天任务数量
  -  12 个月日历简化视图
  - 每个月从 01 开始开始
  - 先每个月从左到右排列 4 个月，再从上到下排列
  - 每天格子尽量收缩，容纳更多的年份
  - 蓝色颜色深浅表示完成任务数量多少，最深颜色为浅色，不要遮蔽日期文字
- 多年视图
  - 就多个单年视图从上到下排列

### 所有任务甘特图

#### 任务树

引用[所有任务树视图](###所有任务树视图)

#### 任务线条

##### 信息

使用任务树中对应任务行的任务信息。

##### 功能

- 显示任务树中任务行对应的任务线条，表示任务时间过渡。
- 显示任务树中任务行之间的依赖关系。

##### UI设计

###### 视图动态调整

- 甘特图视图宽度随页面宽度调整
- 点击查询后将任务条视图调整到合适大小，最长任务线条左右靠近视图边缘
- 点击查询后自动聚焦右侧任务线条

###### 任务树和任务线条关联

- 将左侧的任务树和右侧的任务线条关联
  - 同步折叠，折叠时不显示任务描述，不显示任务线条
  - 同步对齐，同一任务的线条高度始终和对应的任务行对齐。
    - 如：✅ 制作初步音乐库和它的过渡线条要一样高。
    - 线条中心线和任务文字中心线始终水平对齐
      - 左侧任务滑动改变位置后
      - 左侧任务排序改变位置后
- 在查询，折叠，展开，缩放操作后才需要重绘。

###### 甘特图任务线条

- 任务线条形状
  - 高度，默认
  - 长度，根据判断任务时间和日历时间交集的条件选择，开始时间到完成时间或者计划时间到截止时间。
  - 圆角
- 任务线条颜色
  - 线条根据执行状态设置颜色，参考[共用颜色](###共用颜色)
  - 线条颜色适配 obsidian 深色主题，浅色系
- 任务线条说明
  - 线条右侧显示任务线条时间段长度的文字说明
    - 根据判断任务时间和日历时间交集的条件选择，开始时间到完成时间或者计划时间到截止时间
- 任务线条依赖
  - 反映 tasks 任务标记指定的任务依赖关系（甘特图特有的依赖关系）
  - 依赖箭头用折线，带圆角
  - 从标记 ID 任务的完成时间到引用标记 ID 任务的开始时间或者从标记 ID 任务的截止时间到引用标记 ID 任务的计划时间绘制箭头
    - 根据判断交集时选择的开始时间到完成时间或者计划时间到截止时间判断
    - 从 ID 任务线条的完成时间侧中线末端到引用 ID 任务线条的开始时间侧左上角或者从 ID 任务线条的截止时间侧中线末端到引用 ID 任务线条的计划时间侧左上角绘制箭头
    - 当 ID 任务的结束时间和引用 ID 任务的开始时间重合时，为了箭头含义清晰可以向引用 ID 任务的开始时间右侧偏移一点
    - 当 ID 任务的结束时间和引用 ID 任务的开始时间重合时，为了箭头含义清晰可以向引用 ID 任务的开始时间右侧偏移一点

###### 甘特图背景

- 无背景色，填使用 obsidian 主题页面背景色
- 任务线条背景图
  - 为不断向右延伸的日历周循环
  - 周数从左到右排列
  - 时间轴在背景图最上方
    - 时间段可能跨年，应该有多年，应该有多季，应该有多月，应该有多周
  - 日期说明在背景图上方
  - 时间轴在背景视图最上方
  - 滑动条在背景视图最下方
  - 显示今天参考线
- 背景长，开始时间和结束时间为查询条件的最小时间和最大时间，保证覆盖所有任务时间
  - 查询所有任务后，应该找到对应的最小日期和最大日期，据此生成对应的背景视图
- 背景高，和任务树窗口的高度一致

###### 交互

- 鼠标悬停在任务线条上提示完整任务信息
- 深色背景

### 所有任务基础统计图

#### 数据来源

- pages/A 系统/A 任务系统
- 在文件名以任务结尾的文件中查询，排除其他文件

#### 查询任务

- 以今天为参考的时间段汇总任务 
  - 日：昨天、今天、明天、近几天
  - 周：前 1 周、前 2 周
  - 月：前 1 月、前 2 月
  -  - 年：前 1 年、前 2 年

- 以指定时间段汇总任务

  - 日：某天
  - 周：某周
  - 月：某月
  - 季：某季
  - 年：某年

  

#### 统计图表视图

- 视图中任务查询
  - 默认查询本周任务
  - 可以通过查询按钮的查询条件筛选任务，符号条件的任务显示在左侧任务树中，不符合条件的任务隐藏。
  - 可以通过点击有子项的任务进一步筛选任务，统计此任务下所有子任务的数据，并更新图表数据，。
- 动态调整
  - 视图跟随查询结果调整渲染
    - 有查询结果的任务显示示在左侧任务树中。
  - 重绘
    - 在查询条件改变后。
  - 滚动条，视图超出页面宽度滚动条调整
  - 缩放，图表视图部分可按 alt+ 滚轮缩放
- 简单视图
  - 适配 obsidian
    - 背景色适配 obsidian 深色主题
    - 字体
- 简单交互
  - 点击循环按钮显示或隐藏任务树中的循环任务，不显示任务相关信息。默认隐藏循环任务。
  - 点击已完成按钮显示或隐藏任务树中的已完成任务，不显示任务相关信息。默认隐藏已完成任务。
  - 点击已取消按钮显示或隐藏任务树中的已取消任务，不显示任务相关信息。默认隐藏已取消任务。
  - 点击任务交集时间切换按钮切换判断交集的条件，默认计划时间到截止时间
    - 创建时间，开始时间，完成时间，计划时间，截止时间，取消时间有一个时间与日历中日期相同判断为有交集
    - 条件 1，计划时间到截止时间与日历中的日期有交集判断为有交集（无完成时间就用截止时间代替）
    - 条件 2，开始时间到完成时间与日历中的日期有交集判断为有交集（无完成时间就用截止时间代替）
  - 隐藏/显示文件夹按钮，默认隐藏
    - 隐藏后视图以任务文件为节点，减少显示层级
- 图表视图
  - 将左侧的任务树和右侧的图表分离
  - 图表布局
    - 图表布局从左侧起，不要留空白
    - 多个图表从左到右从上到下排列
      - 第 1 行，执行状态，优先级，循环周期，
      - 第 2 行，日期标记，依赖关系，标签
      - 第 3 行，计划时长，执行时长，计划执行
      - 第 4 行，执行状态详细图表
  - 图表颜色
    - 图表文字和颜色有对比度
    - 执行状态（未开始🔲（#2e333b），计划中❔（#4b525b），进行中⏩（#7fb8f0），已完成✅（#47852f），已取消❎（#c3393e））
      - 从淡到浓
    - 优先级（⏬🔽🔼⏫🔺）
      - 从淡到浓
    - 循环周期（🔁 every day 🔁 every week 🔁 every month 🔁 every year）
      - 从淡到浓
    - 任务日期 ➕ 创建日期 ⏳ 计划日期 🛫 开始日期📅 截止日期 ✅ 完成日期 ❌ 取消日期
      - 从淡到浓
  - 图表样式
    - 图表样式切换，
      - 同样的统计数据下的不同图表样式切换
      - 指定图表单独切换
    - 饼图
      - 每个单独数据和总数的关系
    - 堆叠柱状图
      - 堆叠柱状图应该有日期的加入，每个日期对应一条竖线，高为今日任务总数
      - 竖线由多段颜色组成，其中每段颜色表示各自对应的数量和占比，所有颜色对应的数量之和为今日任务总数，
      - 每段颜色的排列顺序为颜色的从淡到浓
  - 图表放大
    - 由于视图中图标比较多，可以通过点击放大缩小图表，方便查看
- 任务树
  - 任务
    - 执行状态 + 任务描述
  - UI
    - 在甘特图视图左侧
    - 任务树窗口
      - 默认长到窗口底部
        - 超出格子长度的任务隐藏
      - 默认宽为固定宽度
      - 宽度调整
        - 点击右侧边缘鼠标拉伸窗口
    - 任务树视图
      - 从任务系统的下一级开始作为顶层，显示任务树。
      - 任务嵌套
      - 折叠展开
      - 最底层的任务列表显示
      - 任务列表的任务描述不换行，通过提示或扩展窗口查看完整信息
      - 有子任务的任务标题不换行
    - 单个任务显示
      - 在树和列表视图中
        - 显示任务描述
      - 在鼠标悬停提示中
        - 其他任务信息每个单独一行：
        - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）优先级（🔺⏫🔼🔽⏬）循环周期（🔁 every day 🔁 every week 🔁 every month 🔁 every year）➕ 创建日期 ⏳ 计划日期 🛫 开始日期📅 截止日期✅ 完成日期❌ 取消日期 🆔 dcf64c（唯一标记）⛔ dcf64c,h17ye（依赖标记可多个）🏁 keep（标签）
    - 任务排序
      - 点击排序按钮为最底层任务排序，各排序互斥
        - 执行状态（正逆序），未开始 → 计划中 → 进行中 → 已放弃 → 已完成 → 非任务
        - 优先级（正逆序），0=🔺, 1=⏫, 2=🔼, 3=🔽, 4=⏬, none=无
        - 时间（正逆序），创建时间 → 开始时间 → 计划时间 → 截止时间 → 取消时间 → 完成时间
    - 任务进度条
      - 有子项的任务标题右侧显示任务进度条
        - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）
        - 每种状态用不同颜色
        - 每种状态占一段根据数量占比
        - 顺序，下到上，对应执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）
        - 右侧显示完成百分比
  - 交互
    - 点击展开收缩子项
    - 点击任务列表任务跳转到源文件任务位置。
    - 鼠标悬停在任务描述上提示完整任务信息
      - 深色背景
- 任务图表
  - 任务执行状态图表，第 1 行
    - 数据
      - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）数量
      - 任务总数
    - 计算
      - 统计数量，计算比例
    - UI
      - 饼图
      - 每种状态用不同颜色
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务优先级图表，第 1 行
    - 数据
      - 优先级（⏬🔽🔼⏫🔺）数量
      - 任务总数
    - 计算
      - 统计数量，计算比例
    - UI
      - 饼图
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务循环图表，第 1 行
    - 数据
      - 循环周期（🔁 every day 🔁 every week 🔁 every month 🔁 every year）数量
      - 任务总数
    - 计算
      - 统计数量，计算比例
    - UI
      - 饼图
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务日期图表，第 2 行
    - 数据
      - ➕ 创建日期 ⏳ 计划日期 🛫 开始日期📅 截止日期 ✅ 完成日期 ❌ 取消日期数量
      - 任务总数
    - 计算
      - 统计数量
      - 计算比例
    - UI
      - 饼图
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务依赖图表，第 2 行
    - 数据
      - 🆔 dcf64c（唯一标记）数量
      - ⛔ dcf64c,h17ye（依赖标记可多个）数量
      - 🆔 dcf64c（唯一标记）和 ⛔ dcf64c,h17ye（依赖标记可多个）数量
      - 任务总数
    - 计算
      - 统计数量，计算比例
    - UI
      - 饼图
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务标签图表，第 2 行
    - 数据
      - 🏁 keep（标签）
      - 任务总数
    - 计算
      - 统计数量，计算比例
    - UI
      - 饼图
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务计划时长图表，第 3 行
    - 数据
      - ⏳ 计划日期 📅 截止日期 时长之和
      - 总任务计划时长，
        - 最小计划日期，最大截止日期计算天数，天数乘以 12 小时
    - 计算
      - 统计数量，计算比例
    - UI
      - 饼图
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务执行时长图表，第 3 行
    - 数据
      - 🛫 开始日期 ✅ 完成日期 时长之和
      - 总任务执行时长
        - 最小开始日期，最大完成日期计算天数，天数乘以 12 小时
    - 计算
      - 统计数量，计算比例
    - UI
      - 饼图
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务执行计划时长图表，第 3 行
    - 数据
      - ⏳ 计划日期 📅 截止日期 时长之和
      - 🛫 开始日期 ✅ 完成日期 时长之和
      - 总任务计划时长，总任务执行时长之和
        - 最小计划日期，最大截止日期计算天数，天数乘以 12 小时
        - 最小开始日期，最大完成日期计算天数，天数乘以 12 小时
    - 计算
      - 统计数量，计算比例
    - UI
      - 饼图
    - 交互
      - 鼠标悬停提示信息
      - 无切换，无放大
  - 任务执行状态详细图表，第 4 行
    - 数据
      - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）数量
      - 任务总数
      - 任务日期
    - 计算
      - 统计数量，计算比例
    - UI
      - 堆叠柱状图
      - 每种状态用不同颜色
      - 顺序，下到上，对应执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）
    - 交互
      - 鼠标悬停提示信息
      - 点击切换放大缩小

### 所有任务详细统计图

##### 数据来源

- pages/A 系统/A 任务系统
- 在文件名以任务结尾的文件中查询，排除其他文件

##### 查询任务

- 以今天为参考的时间段汇总任务 
  - 日：昨天、今天、明天、近几天
  - 周：前 1 周、前 2 周
  - 月：前 1 月、前 2 月
  -  - 年：前 1 年、前 2 年

- 以指定时间段汇总任务

  - 日：某天
  - 周：某周
  - 月：某月
  - 季：某季
  - 年：某年

  

#### 统计图表视图

- 任务查询
  - 默认查询本周任务
  - 可以通过查询按钮的查询条件筛选任务，符号条件的任务显示在左侧任务树中，不符合条件的任务隐藏。
  - 可以通过点击有子项的任务进一步筛选任务，统计此任务下所有子任务的数据，并更新图表数据，。
- 动态调整
  - 视图跟随查询结果调整渲染
    - 有查询结果的任务显示示在左侧任务树中。
  - 重绘
    - 在查询条件改变后。
  - 滚动条，视图超出页面宽度滚动条调整
  - 缩放，图表视图部分可按 alt+ 滚轮缩放
- 简单视图
  - 适配 obsidian
    - 背景色适配 obsidian 深色主题
    - 字体
- 简单交互
  - 点击循环按钮显示或隐藏任务树中的循环任务，不显示任务相关信息。默认隐藏循环任务。
  - 点击已完成按钮显示或隐藏任务树中的已完成任务，不显示任务相关信息。默认隐藏已完成任务。
  - 点击已取消按钮显示或隐藏任务树中的已取消任务，不显示任务相关信息。默认隐藏已取消任务。
  - 点击任务交集时间切换按钮切换判断交集的条件，默认计划时间到截止时间
    - 创建时间，开始时间，完成时间，计划时间，截止时间，取消时间有一个时间与日历中日期相同判断为有交集
    - 条件 1，计划时间到截止时间与日历中的日期有交集判断为有交集（无完成时间就用截止时间代替）
    - 条件 2，开始时间到完成时间与日历中的日期有交集判断为有交集（无完成时间就用截止时间代替）
  - 隐藏/显示文件夹按钮，默认隐藏
    - 隐藏后视图以任务文件为节点，减少显示层级
- 图表视图
  - 将左侧的任务树和右侧的图表分离
  - 图表布局
    - 图表布局从左侧起，不要留空白
    - 多个图表从左到右从上到下排列
      - 第 1 行，执行状态，优先级，循环周期，
      - 第 2 行，日期标记，依赖关系，标签
      - 第 3 行，计划时长，执行时长，计划执行
      - 第 4 行，执行状态详细图表
  - 图表颜色
    - 图表文字和颜色有对比度,参考[共用颜色](###共用颜色)
    - 执行状态（未开始🔲（#2e333b），计划中❔（#4b525b），进行中⏩（#7fb8f0），已完成✅（#47852f），已取消❎（#c3393e））
      - 从淡到浓
    - 优先级（⏬🔽🔼⏫🔺）
      - 从淡到浓
    - 循环周期（🔁 every day 🔁 every week 🔁 every month 🔁 every year）
      - 从淡到浓
    - 任务日期 ➕ 创建日期 ⏳ 计划日期 🛫 开始日期📅 截止日期 ✅ 完成日期 ❌ 取消日期
      - 从淡到浓
  - 图表样式
    - 图表样式切换，
      - 同样的统计数据下的不同图表样式切换
      - 指定图表单独切换
    - 饼图
      - 每个单独数据和总数的关系
    - 堆叠柱状图
      - 堆叠柱状图应该有日期的加入，每个日期对应一条竖线，高为今日任务总数
      - 竖线由多段颜色组成，其中每段颜色表示各自对应的数量和占比，所有颜色对应的数量之和为今日任务总数，
      - 每段颜色的排列顺序为颜色的从淡到浓
  - 图表放大
    - 由于视图中图标比较多，可以通过点击放大缩小图表，方便查看
- 任务树
  - 任务
    - 执行状态 + 任务描述
  - UI
    - 在甘特图视图左侧
    - 任务树窗口
      - 默认长到窗口底部
        - 超出格子长度的任务隐藏
      - 默认宽为固定宽度
      - 宽度调整
        - 点击右侧边缘鼠标拉伸窗口
    - 任务树视图
      - 从任务系统的下一级开始作为顶层，显示任务树。
      - 任务嵌套
      - 折叠展开
      - 最底层的任务列表显示
      - 任务列表的任务描述不换行，通过提示或扩展窗口查看完整信息
      - 有子任务的任务标题不换行
    - 单个任务显示
      - 在树和列表视图中
        - 显示任务描述
      - 在鼠标悬停提示中
        - 其他任务信息每个单独一行：
        - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）优先级（🔺⏫🔼🔽⏬）循环周期（🔁 every day 🔁 every week 🔁 every month 🔁 every year）➕ 创建日期 ⏳ 计划日期 🛫 开始日期📅 截止日期✅ 完成日期❌ 取消日期 🆔 dcf64c（唯一标记）⛔ dcf64c,h17ye（依赖标记可多个）🏁 keep（标签）
    - 任务排序
      - 点击排序按钮为最底层任务排序，各排序互斥
        - 执行状态（正逆序），未开始 → 计划中 → 进行中 → 已放弃 → 已完成 → 非任务
        - 优先级（正逆序），0=🔺, 1=⏫, 2=🔼, 3=🔽, 4=⏬, none=无
        - 时间（正逆序），创建时间 → 开始时间 → 计划时间 → 截止时间 → 取消时间 → 完成时间
    - - - 
  - 交互
    - 点击展开收缩子项
    - 点击任务列表任务跳转到源文件任务位置。
    - 鼠标悬停在任务描述上提示完整任务信息
      - 深色背景
- 任务图表
  - 任务执行状态详细图表，第 4 行
    - 数据
      - 执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）数量
      - 任务总数
      - 任务日期
    - 计算
      - 统计数量，计算比例
    - UI
      - 堆叠柱状图
      - 每种状态用不同颜色,参考[共用颜色](###共用颜色)
      - 顺序，下到上，对应执行状态（未开始🔲，计划中❔，进行中⏩，已完成✅，已取消❎）
    - 交互
      - 鼠标悬停提示信息
      - 点击切换放大缩小

## 持久化设计

将用户配置存储在本地文件中，再次打开时还原配置

## 配置设计

全局配置的常量和变量

变量可以提供给用户，进行插件配置

## {模块名称}

### {模块的自然语言描述}

### 算法描述

```yaml
算法:
  名称: {算法名称}
  复杂度: {时间或空间复杂度}
  伪代码: |
    {算法的伪代码或步骤化自然语言描述}
```

### 事件流

```yaml
事件流:
  步骤:
    - 触发: {触发事件}
      动作: {系统执行的动作}
      下一步: {动作完成后的流转}
    - 触发: {触发事件}
      动作: {系统执行的动作}
      下一步: {动作完成后的流转}
  伪代码: |
    {事件处理的可执行伪代码}
```

### 关键条件

```yaml
关键条件:
  描述: {核心业务逻辑或判断规则}
  相关函数:
    - {受此条件影响的函数名}
  伪代码: |
    {条件逻辑的可执行伪代码}
  边界条件:
    - {边界情况说明}
```

# 需求的可追踪性

# 注解

# 附录

