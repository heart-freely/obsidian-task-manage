{
	"name": "obsidian-task-manage",
	"version": "1.0.0",
	"description": "任务管理面板，基于 tasks、dataview、echarts 插件",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "node esbuild.config.mjs production",
		"test": "jest",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"lint": "eslint ."
	},
	"devDependencies": {
		"@babel/core": "^7.26.0",
		"@babel/preset-env": "^7.26.0",
		"babel-jest": "^30.3.0",
		"esbuild": "^0.25.12",
		"jest": "^30.3.0"
	},
	"dependencies": {
		"echarts": "^6.0.0",
		"obsidian": "latest"
	}
}
